#include <bits/stdc++.h>
#include <iostream>
#include <fstream>

using namespace std;

multiset<int> S[2]; 
int pos[2001];
int numOutcomes;

int main(int argc, char *argv[])   {
//guess

    string inputFile = "guess.in";
    string outputFile = "guess.out";
    if (argc >2) {
           inputFile = argv[1];
           outputFile = argv[2];
     }
     ifstream fin (inputFile);
     ofstream fout (outputFile);

}
